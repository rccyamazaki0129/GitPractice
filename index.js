// Hello, world! を出力する
console.log("Hello, world!");

// マジックナンバーを表示する
const magicNumber = 6478293828;
console.log(`magic number : ${magicNumber}`);

// 名前を指定して，日本語で挨拶する関数
const greeting = (name) => {
  console.log(`はじめまして，${name}様`);
}